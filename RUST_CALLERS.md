# Rust callers — `assert-matches-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `assert_matches` | `assert_matches` | 14 | 8 | 2 | 13 |

## `assert_matches`

### Inbound symbols (1 distinct, 14 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `assert_matches`.

#### `assert_matches::assert_matches` — 14 site(s) (13 codex, 1 other, 8 caller crates)

- ✅ `codex-kotlin/codex-api` `src/sse/responses.rs:593` — `use assert_matches::assert_matches`
- ✅ `codex-kotlin/codex-apply-patch` `src/invocation.rs:381` — `use assert_matches::assert_matches`
- ✅ `codex-kotlin/codex-cli` `src/main.rs:1686` — `use assert_matches::assert_matches`
- ✅ `codex-kotlin/codex-core` `src/agent/control_tests.rs:12` — `use assert_matches::assert_matches`
- ✅ `codex-kotlin/codex-core` `src/config/config_tests.rs:7` — `use assert_matches::assert_matches`
- ✅ `codex-kotlin/codex-core` `tests/suite/abort_tasks.rs:1` — `use assert_matches::assert_matches`
- ✅ `codex-kotlin/codex-core` `tests/suite/tool_harness.rs:4` — `use assert_matches::assert_matches`
- ✅ `codex-kotlin/codex-ollama` `src/parser.rs:33` — `use assert_matches::assert_matches`
- ✅ `codex-kotlin/codex-tui` `src/app/tests.rs:22` — `use assert_matches::assert_matches`
- ✅ `codex-kotlin/codex-tui` `src/app/tests/model_catalog.rs:2` — `use assert_matches::assert_matches`
- ✅ `codex-kotlin/codex-tui` `src/chatwidget/tests.rs:34` — `pub_use assert_matches::assert_matches`
- ✅ `codex-kotlin/codex-tui` `src/chatwidget/tests/status_command_tests.rs:2` — `use assert_matches::assert_matches`
- _… and 2 more site(s)._
