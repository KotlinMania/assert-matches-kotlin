# Port targets — `assert-matches-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `assert_matches` that codex-rs uses, and therefore the minimum subset that `assert-matches-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-14] (rank `0`, tier `direct`, status `To Do`)

**Cargo package name**: `assert_matches`
**Rust module name (in `use` statements)**: `assert_matches`

## Summary

- **1** distinct symbols imported by codex-rs
- **13** total `use` statements
- **13** codex source files affected
- **7** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `assert_matches::assert_matches`

- **13 `use` site(s)** across **7 codex member crate(s)**: `codex-api`, `codex-apply-patch`, `codex-cli`, `codex-core`, `codex-ollama`, …

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `apply-patch/src/invocation.rs:381` (`codex-apply-patch`) — `use assert_matches::assert_matches`
- `cli/src/main.rs:1686` (`codex-cli`) — `use assert_matches::assert_matches`
- `codex-api/src/sse/responses.rs:593` (`codex-api`) — `use assert_matches::assert_matches`
- `core/src/agent/control_tests.rs:12` (`codex-core`) — `use assert_matches::assert_matches`
- `core/src/config/config_tests.rs:7` (`codex-core`) — `use assert_matches::assert_matches`
- `core/tests/suite/abort_tasks.rs:1` (`codex-core`) — `use assert_matches::assert_matches`
- `core/tests/suite/tool_harness.rs:4` (`codex-core`) — `use assert_matches::assert_matches`
- `ollama/src/parser.rs:33` (`codex-ollama`) — `use assert_matches::assert_matches`
- `tui/src/app/tests.rs:22` (`codex-tui`) — `use assert_matches::assert_matches`
- `tui/src/app/tests/model_catalog.rs:2` (`codex-tui`) — `use assert_matches::assert_matches`
- `tui/src/chatwidget/tests.rs:34` (`codex-tui`) — `use assert_matches::assert_matches`
- `tui/src/chatwidget/tests/status_command_tests.rs:2` (`codex-tui`) — `use assert_matches::assert_matches`
- `utils/readiness/src/lib.rs:207` (`codex-utils-readiness`) — `use assert_matches::assert_matches`
